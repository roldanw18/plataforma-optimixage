services:
  # ─────────────────────────────────────────────────────────────
  # Backend FastAPI (Docker)
  # URL: https://optimixage-plataforma-api.onrender.com
  #
  # NOTA: si al hacer "Apply Blueprint" Render rechaza el nombre
  # porque ya está tomado, cambia `name` (y las 2 referencias a la
  # URL del frontend / backend más abajo) por algo único, por
  # ejemplo "optimixage-app" o el nombre que prefieras.
  # ─────────────────────────────────────────────────────────────
  - type: web
    name: optimixage-plataforma-api
    runtime: docker
    plan: free
    region: oregon
    rootDir: backend
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        sync: false
      - key: BOOTSTRAP_ADMIN_PASSWORD
        sync: false
      - key: JWT_ALGORITHM
        value: HS256
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: "60"
      - key: ENVIRONMENT
        value: production
      - key: CORS_ORIGINS
        value: https://optimixage-plataforma.onrender.com
      - key: BOOTSTRAP_ADMIN_EMAIL
        value: admin@optimixage.local
      - key: BOOTSTRAP_ADMIN_NOMBRE
        value: Administrador

  # ─────────────────────────────────────────────────────────────
  # Frontend React (Static Site — nunca se duerme)
  # URL: https://optimixage-plataforma.onrender.com
  # ─────────────────────────────────────────────────────────────
  - type: web
    name: optimixage-plataforma
    runtime: static
    rootDir: frontend
    buildCommand: npm install --include=dev && npm run build
    staticPublishPath: ./dist
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
      - key: VITE_API_URL
        value: https://optimixage-plataforma-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    headers:
      - path: /*
        name: X-Frame-Options
        value: SAMEORIGIN
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
